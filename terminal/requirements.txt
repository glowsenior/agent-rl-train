# Terminal Agent Training Requirements
# =====================================

# Core dependencies
torch>=2.0.0
transformers>=4.40.0
accelerate>=0.30.0
peft>=0.11.0

# Training frameworks
trl>=0.15.0
vllm>=0.6.0

# ART (OpenPipe Agent Reinforcement Trainer)
# Install with: pip install "openpipe-art[backend]"
openpipe-art[backend]

# Data processing
datasets>=2.18.0
pyyaml>=6.0
jinja2>=3.1.0

# Logging and monitoring
wandb>=0.16.0
tensorboard>=2.16.0

# Async and networking
aiohttp>=3.9.0
httpx>=0.27.0
nest-asyncio>=1.6.0

# Docker integration (for affinetes)
docker>=7.1.0

# Environment management
python-dotenv>=1.0.0

# Utilities
tqdm>=4.66.0
tabulate>=0.9.0
